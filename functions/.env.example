# Cloud Functions の環境変数 (firebase-functions が functions/.env を読み込む)。

# 中央 CRM / ポイント台帳の受信 Webhook (§10)。
# CRM 構築前は空のままで可 → イベントは pointEvents に pending で貯まり、
# URL 設定後に retryPointEvents が自動で配信する。
CRM_WEBHOOK_URL=
